- For changing the color of the button/other elements edit `tailwind.config.js` file 
- Then run the command (if you are using bun) after installing tailwindcss
    ```bash
    bun tailwindcss -i ./input.css -o ./style.css --watch
    ```